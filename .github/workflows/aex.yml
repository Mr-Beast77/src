name: src
on: push

jobs:
  src:
    name: aex
    runs-on: macos-latest
    env:
      rclone_config: ${{ secrets.RCLONE_CONFIG }}
    steps:
    - uses: actions/checkout@v2

    - name: shell
      run: echo $0
